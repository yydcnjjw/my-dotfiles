#!/bin/bash

if command -v systemctl >/dev/null 2>&1; then
    systemctl --user daemon-reload
    {{- if .install_ttrss }}
    echo "Enabling and starting ttrss.service for user..."
    systemctl --user enable --now ttrss.service
    {{- end }}
fi
